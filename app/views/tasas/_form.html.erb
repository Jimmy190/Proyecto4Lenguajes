<%= form_with(model: tasa) do |form| %>
  <% if tasa.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tasa.errors.count, "error") %> prohibited this tasa from being saved:</h2>

      <ul>
        <% tasa.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nombre, style: "display: block" %>
    <%= form.text_field :nombre %>
  </div>

  <div>
    <%= form.label :porcentaje, style: "display: block" %>
    <%= form.text_field :porcentaje %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
